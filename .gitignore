# Vendored shader collection cloned with its own .git — not tracked here
.config/ghostty/ghostty-shaders/

# Local pre-migration snapshots, kept out of version control
backups/

# Machine-local secrets — only .env.example is tracked
.env
# generated per machine by omni-ghostty-theme on every theme switch
.config/ghostty/omni-theme
