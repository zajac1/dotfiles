# Machine-local secrets/vars, sourced by .zshrc on shell start.
# Copy this file to ~/.env and add whatever keys this machine needs.
# ~/.env is gitignored — never commit real secrets. Keys are per-machine,
# so this template intentionally lists none.
#
# Example:
# export SOME_API_KEY=your-value-here
